# 📋 할일 위젯 (Todo Widget)

> 포스트잇 스타일의 가벼운 데스크탑 할일 관리 위젯

![Python](https://img.shields.io/badge/Python-3.x-blue?logo=python&logoColor=white)
![Platform](https://img.shields.io/badge/Platform-Windows-0078D6?logo=windows&logoColor=white)
![License](https://img.shields.io/badge/License-Private-red)

---

## ✨ 주요 기능

- 📌 **항상 위에 고정** — 다른 창 위에 항상 떠있는 위젯
- 🗂 **시스템 트레이** — 닫기 버튼을 눌러도 트레이로 숨겨져 백그라운드에서 계속 실행
- 💾 **자동 저장** — 추가·체크·삭제·수정 즉시 자동으로 저장
- 📅 **미완료 항목 유지** — 날짜가 지나도 완료하지 않은 항목은 사라지지 않음 (`N일 전` 뱃지 표시)
- ✏️ **수정 기능** — 항목별 ✏ 버튼 또는 더블클릭으로 팝업 편집창 열기
- 🔀 **드래그 정렬** — `≡` 핸들을 드래그해 항목 순서 변경 (우선순위 지정)
- 🖱 **전체 스크롤** — 항목 위에 마우스를 올린 상태에서도 휠 스크롤 동작
- ↔️ **자유로운 창 크기 조절** — 우하단 코너 / 우측 엣지 / 하단 엣지 드래그로 크기 변경
- 🔍 **전체 목록 관리 창** — 필터(전체/미완료/완료) + 검색 기능
- 🎨 **5가지 테마** — 노랑(기본) / 파랑 / 초록 / 분홍 / 다크모드
- 🚀 **시작프로그램 등록** — 윈도우 부팅 시 자동 실행 옵션
- 💼 **백업 / 불러오기** — 할일 데이터를 파일로 내보내거나 가져오기

---

## 🖥 UI 미리보기

```
┌──────────────────────────────┐
│ 📋 2026.04.06 (월)       ─ × │  ← 드래그로 이동 가능
├──────────────────────────────┤
│ ☐ 회의 자료 준비       (어제)│
│ ☑ 이메일 확인               │  ← 완료 시 취소선
│ ☐ 운동하기                  │
│  2/3 완료   완료삭제   목록  │
├──────────────────────────────┤
│  할일 입력...           [ + ]│
└──────────────────────────────┘
```

---

## 🚀 실행 방법

### 방법 1 — Python으로 바로 실행

```bash
pip install pystray Pillow
python todo_widget.py
```

### 방법 2 — EXE 빌드 후 실행

```bash
pip install pystray Pillow pyinstaller
build.bat
# 또는
pyinstaller --onefile --noconsole --name "할일위젯" todo_widget.py
```

빌드 결과물: `dist/할일위젯.exe`

---

## 📁 파일 구조

```
todo-widget/
├── todo_widget.py   # 메인 소스 코드 (단일 파일)
├── build.bat        # EXE 빌드 스크립트
├── todos.json       # 할일 데이터 (자동 생성)
└── README.md
```

> `todos.json`은 실행 파일과 같은 폴더에 자동으로 생성됩니다.

---

## ⚙️ 사용법

| 동작 | 방법 |
|------|------|
| 할일 추가 | 입력창 타이핑 후 `Enter` 또는 `+` 버튼 |
| 완료 체크/해제 | 항목 클릭 |
| 텍스트 수정 | `✏` 버튼 클릭 또는 항목 **더블클릭** |
| 순서 변경 | `≡` 핸들을 **드래그** |
| 항목 삭제 | 우측 `×` 버튼 |
| 창 크기 조절 | 우하단 `◢` / 우측 엣지 / 하단 엣지 드래그 |
| 창 숨기기 | `─` 또는 `×` → 트레이로 최소화 |
| 다시 열기 | 트레이 아이콘 **더블클릭** |
| 설정 변경 | 헤더 **우클릭** 또는 트레이 아이콘 우클릭 |
| 전체 목록 | 하단 `목록` 버튼 |
| 백업 저장 | 헤더 우클릭 → `백업 (내보내기)` |
| 백업 불러오기 | 헤더 우클릭 → `불러오기 (가져오기)` |

### 설정 메뉴 (헤더 우클릭)

- ✅ 항상 위에 표시 토글
- 🔆 투명도 조절 (75% / 85% / 95% / 100%)
- 🎨 테마 색상 변경 (노랑★ / 파랑 / 초록 / 분홍 / 다크)
- 🚀 윈도우 시작 시 자동 실행 등록/해제
- 📂 전체 목록 창 열기
- 💼 백업 (내보내기) / 불러오기 (가져오기)
- ❌ 완전 종료

---

## 📦 의존성

| 패키지 | 용도 | 설치 필요 |
|--------|------|-----------|
| `tkinter` | GUI 프레임워크 | ❌ Python 내장 |
| `pystray` | 시스템 트레이 아이콘 | ✅ `pip install pystray` |
| `Pillow` | 트레이 아이콘 이미지 생성 | ✅ `pip install Pillow` |
| `pyinstaller` | EXE 빌드 (빌드 시에만) | ✅ `pip install pyinstaller` |

---

## 🗃 데이터 구조 (todos.json)

```json
{
  "window":        { "x": 100, "y": 100, "width": 280, "height": 420 },
  "theme":         "yellow",
  "always_on_top": true,
  "opacity":       0.95,
  "startup":       false,
  "todos": [
    { "id": 1, "text": "회의 자료 준비", "done": false, "created_date": "2026-04-05" },
    { "id": 2, "text": "이메일 확인",    "done": true,  "created_date": "2026-04-06" }
  ]
}
```

---

## 📝 변경 로그

### v1.5.0 — 2026-04-07
- 🎨 **전체 디자인 리뉴얼** — 토스 UI 스타일로 재설계 (흰 카드, 블루 계열 기본 테마, 깔끔한 타이포)
- 🃏 **카드 UI 고도화** — 흰 카드 + 서브 컬러 테두리, 체크박스를 ○/● 심벌로 교체
- 🔵 **기본 테마 변경** — 노랑 → 블루 (Toss 스타일), 5가지 테마 모두 색상 리뉴얼
- 📜 **스크롤 상단 여백 완전 수정** — sf Configure 이벤트 width/height로 직접 scrollregion 설정 (bbox 오프셋 버그 완전 제거)
- ↔️ **리사이즈 완전 수정** — 우측 6px 핸들(내부 Canvas로 collapse 방지), 하단 10px strip, 대각 ◢ 그립 모두 정상 작동
- 🖥 **트레이 메뉴 상태 표시** — ✅/⬛ 이모지로 켜짐/꺼짐 실시간 표시

### v1.4.0 — 2026-04-07
- 📜 **스크롤 상단 여백 수정** — 맨 위로 스크롤 시 불필요한 여백이 생기던 버그 수정 (scrollregion 강제 0,0 기준)
- ↔️ **우측 엣지 리사이즈 복구** — 우측 4px 핸들로 가로 크기 조절 가능, 세로·대각 포함 3방향 모두 지원
- ⚡ **리사이즈 깜빡임 개선** — wraplength 업데이트 디바운스(80ms)로 드래그 중 렌더링 부하 감소
- 🖥 **트레이 메뉴 상태 표시** — ✅/⬛ 이모지로 현재 켜짐/꺼짐 상태를 메뉴 열 때마다 실시간 표시
- 🚫 **날짜 뱃지 제거** — 메인 위젯에서 `(어제)`, `(N일 전)` 표시 삭제 (텍스트 공간 확보)
- ↔️ **동적 줄바꿈** — 창 너비를 늘리거나 줄이면 텍스트가 자동으로 재배치
- 🃏 **카드 박스 UI** — 항목마다 테두리 박스를 적용해 가독성 향상
- 🔧 **인라인 수정** — 수정 버튼 클릭 시 팝업 대신 해당 줄에서 바로 편집, 우측에 ✓(완료) / ✗(취소) 버튼 추가
- 🖥 **트레이 메뉴 설정 확장** — 항상 위에 표시 / 자동 실행 토글 · 백업/불러오기를 트레이 우클릭에서도 접근 가능

### v1.1.0 — 2026-04-07
- ✏️ **할일 수정 기능** — 항목별 ✏ 버튼 추가, 팝업 편집창으로 수정
- 🔀 **드래그 정렬** — `≡` 핸들 드래그로 항목 순서(우선순위) 변경
- 🖱 **스크롤 버그 수정** — 항목 위에 마우스를 올린 상태에서도 휠 스크롤 동작
- 📜 **스크롤바 개선** — 항상 표시되도록 개선, 너비 조정
- ↔️ **창 크기 조절 개선** — 우측 엣지 / 하단 엣지 드래그 추가 (코너만 가능하던 문제 수정)
- 🚀 **시작프로그램 수정** — 레지스트리 키 ASCII화, `pythonw.exe` 경로 자동 감지
- 💼 **백업 / 불러오기** — 할일 데이터를 JSON 파일로 내보내기·가져오기

### v1.0.0 — 2026-04-06
- 최초 릴리즈
- 포스트잇 스타일 위젯, 시스템 트레이, 5가지 테마, 전체 목록 관리 창
